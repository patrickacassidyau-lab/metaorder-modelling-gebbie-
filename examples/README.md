# Examples

| Script | Role |
|--------|------|
| `synthetic_demo.py` | Minimal random tick demo + backtest. |
| `generate_synthetic_market.py` | Large synthetic panels — prefer **`--output-dir`** for big jobs (one file per symbol + `manifest.json`). |

**Plots / charts** live under **`../visualisation/`** — see `visualisation/README.md`.
